from calibre.web.feeds.news import BasicNewsRecipe

class WaPoMix(BasicNewsRecipe):
    title = 'The Washington Post (Mix)'
    language = 'en'
    max_articles_per_feed = 25
    oldest_article = 1
    use_embedded_content = False

    feeds = [
        ('Politics', 'http://feeds.washingtonpost.com/rss/politics'),
        ('World',    'http://feeds.washingtonpost.com/rss/world'),
        ('Tech',     'http://feeds.washingtonpost.com/rss/business/technology'),
    ]

    def get_browser(self, *args, **kwargs):
        br = super().get_browser(*args, **kwargs)
        br.addheaders = [
            ('User-Agent', 'Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 '
                           '(KHTML, like Gecko) Chrome/120.0 Safari/537.36'),
            ('Accept-Language', 'en-US,en;q=0.9'),
        ]
        return br

    remove_tags = [
        dict(name=['script', 'style', 'noscript']),
    ]
